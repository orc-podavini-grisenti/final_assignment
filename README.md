To start 

1) build an .venv python envirometn
2) install the reuirements 

3) Before run the scripts:
    - source .venv/bin/activate
    - export PYTHONPATH=$PYTHONPATH:.
    - run all from the root 
